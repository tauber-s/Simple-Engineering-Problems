export class Mosquito {
  constructor(x, y) {
    this.x = x;
    this.y = y;
    this.movesAlive = 0;
    this.alive = true;
  };

  move() {
    if (!this.alive) return;
    
    //const directions = ???
  };
};
